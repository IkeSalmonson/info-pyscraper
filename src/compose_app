import asyncio
from scraper import aiomultiprocess_main , asyncio_main
from info_parser import InfoParse
import sys 

if __name__ == '__main__':
 
   
    
    input_string = sys.stdin.read()
    #print(input_string)  

    scrape_urls , not_url = InfoParse().format_input(input_string)    

    asyncio.run(asyncio_main(scrape_urls))
    #asyncio.run(aiomultiprocess_main(scrape_urls))

    #print('info_list' , info_list)


    print('\n end of compose_app')
    sys.exit(0)
    