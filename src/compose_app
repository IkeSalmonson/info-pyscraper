import asyncio
from scraper import PyScraper, asyncio_main
from info_parser import InfoParse

if __name__ == '__main__':

    
    

    import sys
    print('PyScraper()')
    scraper = PyScraper()
    
    input_string = sys.stdin.read()
    #print(input_string)

   
  

    url_to_scrape , not_url = InfoParse().format_input(input_string)
    
    #scrape_url = PyScraper().request(url_to_scrape)

    


    phone_list = asyncio.run(asyncio_main(url_to_scrape))

    print('phone_list' , phone_list)


    print('\n end of compose_app')
    sys.exit(0)
    